# MiniSQL V3 最终验收报告（阶段版）

日期：2026-09-10

## 1. 验收结论

本报告是基于当前代码库和本轮实际运行结果的阶段验收报告。成员 C 的 X24、X27 关键实现已进入可复现回归链路，成员 A/B 的派生表、诊断、schema versioning、页级 B+ 树和 WAL/checkpoint 改动也已合并并完成 Windows Release 回归；但 X01-X27 仍存在部分实现项，因此整体结论为：

**V3 开发中，成员 C 阶段交付通过，整体最终验收未通过。**

不能把专项测试通过解释为所有扩展完成，也不能把工作台能展示字段解释为后端能力已经闭合。

## 2. 本轮实现范围

### 2.1 X24 权限与审计

- 访问目录使用页式文件 `access.catalog.pages`，支持版本号、重载和原子写入。
- `createUser`、`dropUser`、`createRole`、`dropRole`、`setPassword`、角色绑定/解绑、`grant`、`revoke` 均先复制、校验、再一次性提交，不保留部分变更。
- HTTP bridge 提供独立用户、角色、密码、角色绑定、授权和撤权端点，并要求 `GRANT` 权限。
- CLI 只通过 HTTP bridge 执行，支持 `--execute`、`--compile`、`--file`、`--user`、`--password`，自动创建和关闭会话。
- 工作台提供用户、角色、会话和审计面板，并在请求中携带当前连接身份。

### 2.2 C2 工作台

- 连接配置增加用户和密码，用户身份保存到本地存储并显示在顶栏。
- 会话面板定时刷新活动会话，显示锁等待、持锁状态和当前会话；活动请求支持取消。
- 设置页增加资源预算、备份列表、全量备份和恢复操作入口。
- 不新增 Demo 展示内容，不依赖图片或图片识别能力。
- `tests/c2-dom.cjs` 已通过真实 Edge 无头浏览器回归，覆盖连接配置保存/删除/连接测试失败与恢复、密码不落盘、身份请求头、权限/审计/会话页签、设置页字段、侧栏边界拖动和 390px 视口无横向溢出。

### 2.3 X27 Fuzz

- `fuzz-state-machine.mjs` 生成受状态约束的 CREATE、INSERT、UPDATE、DELETE、SELECT、事务和索引程序，并支持错误探针。
- `fuzz-state-machine-differential.mjs` 使用真实 C++ session 与 Node `node:sqlite` 做共同子集差分。
- `fuzz-state-machine-long-run.mjs` 使用固定种子 `20260908,42`、每种子 512 步执行长跑；`date-literal-process.mjs` 覆盖 DATE/BOOL 字面量类型、索引更新和 DECIMAL 持久化回归。
- 评估器对不可比较 SELECT 只验证真实引擎接受性，不误调用空的参考 SQL。
- CI 增加纯 Node 契约、Windows 引擎构建、HTTP 回归、SELECT 差分和状态机差分。
- 失败状态机 artifact 会保存完整 `program`，`FUZZ_STATE_REPLAY` 可以按 artifact 重放并检查故障分类；固定种子语料另有逐文件复现契约。

## 3. 实际验证记录

| 验证命令 | 结果 | 证据 |
| --- | --- | --- |
| `node tests/access-store-contract.mjs` | 48 项通过 | 页式访问目录契约 |
| `node tests/access-catalog-atomic-contract.mjs` | 27 项通过 | 原子用户/角色/授权接口 |
| `node tests/access-control-http.mjs` | 37 项通过 | 身份、对象权限、元数据和审计过滤 |
| `node tests/access-atomic-http.mjs` | 39 项通过 | 原子权限 HTTP 端点 |
| `node tests/cli-contract.mjs` | 11 项通过 | 权限感知 CLI 契约 |
| `node tests/session-process.mjs` | 51 项通过 | session 跨进程协议与生命周期 |
| `node tests/fuzz-differential.mjs` | 500 项通过 | SELECT 固定种子差分 |
| `FUZZ_STATE_SEEDS=20260908 FUZZ_STATE_STEPS=30 node tests/fuzz-state-machine-differential.mjs` | 23 项通过 | 快速状态机回归 |
| `node tests/fuzz-state-machine-differential.mjs` | 64 项通过 | 3 种子 × 96 步完整状态机回归 |
| `node tests/fuzz-state-machine-long-run.mjs` | 通过 | 2 种子 × 512 步，错误差异/错误接受/错误拒绝/崩溃/超时均为 0 |
| `node tests/date-literal-process.mjs` | 13 项通过 | DATE/BOOL 规划类型、索引 UPDATE 和 DECIMAL 结果 |
| `node tests/derived-smoke.mjs` | 12 项通过 | 派生表解析、作用域绑定和执行 |
| `node tests/diagnostics-smoke.mjs` | 22 项通过 | 批量诊断和恢复模式 tokenizer |
| `node tests/parser-regression.mjs` | 18 项通过 | 解析器/词法器回归与 schema versioning |
| `node tests/index-smoke.mjs` | 29+41+44 项通过 | 页级 B+ 树索引、持久化和重启 |
| `node tests/journal-process.mjs` | 88 项通过 | 累积 WAL、恢复、损坏、身份、锁和旧格式迁移 |
| `node tests/auto-checkpoint-smoke.mjs` | 62 项通过 | 自动 checkpoint 触发与重启一致性 |
| `node tests/x22-fault-injection.mjs` | 30 项通过 | 五阶段故障注入、恢复和重复读取 |
| `node tests/session-http.mjs` | 28 项通过 | 跨请求事务、关闭和空闲事务回滚 |
| `node tests/multi-session-http.mjs` | 44 项通过 | 多会话锁等待、超时和关闭回滚 |
| `node tests/cancel-smoke.mjs` | 20 项通过 | 取消、会话存活和临时文件清理 |
| `node tests/fuzz-state-machine-replay-contract.mjs` | 通过 | 持久化程序重放入口和分类检查 |
| `node tests/fuzz/reproducibility-contract.mjs` | 通过 | 固定种子 SQL 语料逐文件复现 |
| `npm.cmd run test:browser`（工作台） | 通过 | 编译工作台 + 连接/权限/设置/拖拽/移动端 Edge DOM 回归 |
| `npm.cmd run build` | 通过 | React/TypeScript/Vite 构建 |

完整状态机报告目录：`outputs/minisql-backend/tests/artifacts/fuzz-state-eibl5L/`。

## 4. 当前边界与风险

1. 访问目录虽然已经使用页式落盘格式，但还没有成为 C++ 内部 Catalog 的系统表；因此 X24 只能判定为部分实现。
2. 直接启动 `minisql_database.exe` 仍不接受身份参数并执行权限检查；面向用户的受控入口是 HTTP bridge 和权限感知 CLI。
3. SQL 对象识别仍是轻量扫描，CTE、派生表和复杂子查询的对象身份还不是完整 AST 绑定结果。
4. X27 使用共同支持子集进行差分，尚未覆盖无限输入、生产压力、崩溃恢复组合和长期资源曲线。
5. 工作台真实浏览器回归已经通过，但备份创建/恢复的成功与失败仍主要由后端专项测试覆盖，浏览器脚本当前只校验设置入口和禁用条件，没有替换真实数据库文件做破坏性恢复。
6. Vite 仍提示单包约 737 kB，属于性能优化项，不阻断当前构建和功能验收。
7. `origin/builder-A`、`origin/builder-b` 的有效提交已进入当前 `main`；历史 `origin/feature/member-c-x24-x27` 已由 `origin/main` 的 rebased C 提交覆盖，不重复合并。

## 5. 后续验收顺序

1. 在独立测试数据库上执行工作台 DOM 回归，确认身份头、权限面板、锁等待取消、资源预算和备份恢复的真实接口链路。
2. 在 Windows CI 或本机重新构建 Release，引擎回归 job 运行 `database-http`、`access-control-http`、`fuzz-differential` 和状态机差分。
3. 由各成员补齐 X01-X23、X25-X26 的组合证据，并把命令、版本、数据库路径和 artifact 写入对应 progress 文档。
4. 只有当每个 X 编号同时满足代码、接口、成功路径、失败路径以及适用的重启/并发验证时，才将 feature matrix 的状态改为“已实现”。
